/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package tema6;

/**
 *
 * @author Víctor Gabriel Carvajal Aróstegui
 */
public class Lambda {

   /* @FunctionalInterface
    interface Operaciones<T> {

        String operacion(T a, T b);
        String nofunciona(T a, T b);
        
        @FunctionalInterface  //Sólo es un aayuda, no es obligatorio SOLO UN MÉTODO ABSTRACTO
        interface Integrable{
            
        }
        
        Utilidad.integrar(0,1, x->2*x);
    }

    public static void mostrarResultado(int x, int y, Operaciones op) {
        System.out.println(op.operacion(x, y));

    }

    public static void main(String[] args) {
        Operaciones<Integer> suma = (a, b) -> "x + y=" + (a + b);
        Operaciones<Integer> resta = (a, b) -> "x - y=" + (a - b);
        Operaciones<Integer> multiplica = (a, b) -> "x * y=" + (a * b);
        mostrarResultado(2, 3, suma);
        mostrarResultado(5, 1, resta);
        mostrarResultado(4, 7, multiplica);
    }

    String nofunciona(T a, T b);

    Operaciones<Integer> m1 = new Operaciones<>() {
        @Override
        public String operacion(Integer a, Integer b) {
            return "x * y = " + (a * b);
        }
        @Override
        public String nofunciona(Integer id, Integer id2){
            return null;
        }
    };*/
    
    

}
